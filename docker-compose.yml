services:
  mhl-homes-web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Aquí pones la URL real de tu backend en AWS
        - EXPO_PUBLIC_API_URL=https://mhlhomes.com/v1
    ports:
      - "8081:80"   # Puerto estándar HTTP
    restart: always